apiVersion: v1
kind: Service
metadata:
  name: {{ include "simlab-web.fullname" . }}
  labels:
    {{- include "simlab-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simlab-web.selectorLabels" . | nindent 4 }}
